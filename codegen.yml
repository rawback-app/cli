overwrite: true
schema: './graphql.schema.json'
documents: 'src/schema/**/*.gql'
ignoreNoDocuments: true
generates:
  src/gql/:
    preset: client
    presetConfig:
      persistedDocuments: true
    config:
      useTypeImports: true
      scalars:
        Time: string
