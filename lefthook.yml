pre-commit:
  jobs:
    - name: format
      glob: '*.{js,json,md,ts,yaml,yml}'
      run: bunx oxfmt --write {staged_files}
      stage_fixed: true

    - name: lint
      glob: '*.{js,ts}'
      run: bunx oxlint {staged_files}
